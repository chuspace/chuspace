development:
  adapter: redis
  url: <%= ENV.fetch('REDIS_URL') %>

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch('REDIS_URL') %>
  channel_prefix: chuspace_production
  ssl_params:
    verify_mode: <%= OpenSSL::SSL::VERIFY_NONE %>
