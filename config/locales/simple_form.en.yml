en:
  form:
    'yes': 'Yes'
    'no': 'No'
    required:
      text: 'required'
      mark: '*'
      html: '<abbr title="required" class="ml-2 text-error">*</abbr>'
    error_notification:
      default_message: 'Please review the problems below:'
    placeholders:
      user:
        email: Type your email
      post:
        blob_path: read-about-chuspace.md

    labels:
      defaults:
        password: 'Password'
      blog:
        repo_posts_dir: Posts directory
        repo_drafts_dir: Drafts directory (optional)
        repo_assets_dir: Assets directory
        repo_readme_path: Readme
      post:
        blob_path: File name

    hints:
      blog:
        framework: static site generator framework to be used to generate blog from
        visibility: visibility of blog - public, internal or private
        repo_posts_dir: your blog posts will be stored here
        repo_drafts_dir: use a different directory for storing blog drafts
        repo_assets_dir: your blog images would be stored here
        personal: use to connect to your personal profile
      user:
        email: Your email is safe with us
        username: Username should be maximum 40 characters
      post:
        blob_path: filename should end with .md extension

    include_blanks:
      defaults:
        age: 'Rather not say'
    prompts:
      defaults:
        age: 'Select your age'
