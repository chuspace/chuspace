en:
  form:
    'yes': 'Yes'
    'no': 'No'
    required:
      text: 'required'
      mark: '*'
      html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: 'Please review the problems below:'
    placeholders:
      storage:
        description: What's this storage for?
        endpoint: API endpoint for self-hosted or enterprise instances
        access_token: Personal access token generated for selected provider
    labels:
      defaults:
        password: 'Password'
      git_repo:
        fullname: Repository full path
    hints:
      storage:
        access_token: the access token should have following scopes
      blog:
        framework: static site generator framework to be used to generate blog from
        visibility: visibility of blog - public, internal or private
      git_repo:
        fullname: Repository full path

    include_blanks:
      defaults:
        age: 'Rather not say'
    prompts:
      defaults:
        age: 'Select your age'
