en:
  form:
    'yes': 'Yes'
    'no': 'No'
    required:
      text: 'required'
      mark: '*'
      html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: 'Please review the problems below:'
    placeholders:
      storage:
        description: What's this storage for?
        endpoint: API endpoint for self-hosted or enterprise instances
    labels:
      defaults:
        password: 'Password'
      publication:
        git_repository_fullname: 'Select Github repository'
    hints:
      publication:
        git_repository_fullname: 'This repository will be used to store articles for your blog'
      storage:
        access_token: the access token should have following scopes

    include_blanks:
      defaults:
        age: 'Rather not say'
    prompts:
      defaults:
        age: 'Select your age'
