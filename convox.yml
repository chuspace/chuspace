balancers:
  other:
    service: web
    ports:
      443: https:3000

resources:
  database:
    type: postgres

services:
  worker:
    build: .
    command: bundle exec good_job start
    resources:
      - database
  web:
    build: .
    port: https:3000
    resources:
      - database
