resources:
  database:
    type: postgres

services:
  worker:
    build: .
    command: bundle exec good_job start
    resources:
      - database
  web:
    build: .
    domain: chuspace.com,www.chuspace.com
    port: 3000
    resources:
      - database
