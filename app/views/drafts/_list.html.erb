<div class='card bordered'>
  <% if drafts.any? %>
    <%= render partial: 'drafts/item', collection: drafts, as: :draft, cached: true %>
  <% else %>
    <div class='text-center text-2xl py-16'>No drafts in this blog</div>
  <% end %>
</div>
