<main class='h-full block sm:flex items-center justify-between'>
  <section class='w-full sm:w-2/3 pr-32'>
    <h1 class='font-semibold'><%= t('.heading') %></h1>
    <p class="mt-6 text-lg text-secondary">
      <%= t('.intro') %>
    </p>

    <%= link_to 'Know more', '#', class: 'button button--default mt-8 inline-block' %>
  </section>

  <div class='card card-default w-1/3'>
    <%= render 'sessions/identities' %>
  </div>
</main>
