<!DOCTYPE html>
<html lang='en' class='h-full'>
  <%= render 'layouts/head' %>
  <body class="flex flex-col h-full">
    <%= render 'layouts/flash' %>
    <%= render 'publications/drafts/header', draft: @draft %>
    <%= render 'publications/drafts/collaborators', members: @collaboration_session.members.order(:id) %>
    <section class="container flex-1 sm:mt-24">
      <%= yield %>
    </section>
  </body>
</html>
