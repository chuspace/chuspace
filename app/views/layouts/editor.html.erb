<!DOCTYPE html>
<html lang='en' class='h-full'>
  <%= render 'layouts/head' %>
  <body class="flex flex-col h-full">
    <%= render 'layouts/flash' %>
    <%= render 'publications/drafts/header', draft: @draft %>
    <%= turbo_stream_from @collaboration_session %>
    <%= render 'publications/drafts/collaborators', members: @collaboration_session.members %>
    <section class="container flex-1 sm:mt-24">
      <%= yield %>
    </section>
  </body>
</html>
