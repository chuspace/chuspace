<!DOCTYPE html>
<html lang='en' class='h-full'>
  <%= render 'layouts/head' %>
  <body class="flex flex-col h-full">
    <%= render 'layouts/flash' %>
    <%= render 'layouts/header' %>

    <section class="container flex-1 mt-24">
      <%= yield %>
    </section>

    <div class='container mx-auto flex-shrink-0 mt-32'>
      <%= render 'layouts/footer' %>
    </div>
  </body>
</html>
