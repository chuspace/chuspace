<!DOCTYPE html>
<html lang='en' class='h-full'>
  <%= render 'layouts/head' %>
  <body class="flex flex-col h-full">
    <%= render 'layouts/flash' %>
    <%= render 'layouts/header' %>

    <main class="container flex-1 sm:mt-24">
      <section class='lg:w-2/3 mx-auto w-full'>
        <%= yield %>
      </section>
    </main>
  </body>
</html>
