<div class='flex items-center'>
  <div class="dropdown dropdown-end">
    <div tabindex="0" class="m-1 cursor-pointer mask mask-circle"><%= image_tag Current.user.avatar_url %></div>
    <ul tabindex="0" class="shadow menu dropdown-content bg-base-100 rounded-box w-52">
      <li>
        <%= content_tag :div, "Signed in as <strong>#{Current.user.username}</strong>".html_safe, role: 'menuitem', class: 'px-4 py-4 text-current', tabindex: '-1' %>
      </li>
      <div class='divider my-0 h-0'></div>
      <li>
        <%= link_to 'Your profile', user_path(Current.user), class: link_class(user_path(Current.user)), role: 'menuitem', tabindex: '-1' %>
      </li>
      <li>
        <%= link_to 'Blog storages', storages_path, class: link_class(storages_path), role: 'menuitem', tabindex: '-1' %>
      </li>
      <li>
        <%= link_to 'Blogs', blogs_path, class: link_class(blogs_path), role: 'menuitem', tabindex: '-1' %>
      </li>
      <div class='divider my-0 h-0'></div>
      <li>
        <%= button_to 'Sign out', session_path(Current.user), class: 'px-4 py-4 text-current', form: { role: 'menuitem', tabindex: '-1' }, method: :delete, remote: true %>
      </li>
    </ul>
  </div>
</div>
