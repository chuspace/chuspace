<div class='sm:fixed sm:inset-y-1/2 sm:ml-4 sm:mt-0'>
  <%= turbo_frame_tag(:collaboration_session_members) do %>
    <div class="-space-y-3">
      <% members.each do |member| %>
        <div class='flex items-center'>
          <%= render 'publications/drafts/collaborator', member: member %>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
