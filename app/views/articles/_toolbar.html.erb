<div class='bg-gray-100 rounded p-2 mt-4'>
  <% 6.times.each do |index| %>
    <%= link_to "h#{index + 1}", '#', class: 'bg-white border border-gray-200 rounded px-2 text-sm inline-block' %>
  <% end %>
</div>
