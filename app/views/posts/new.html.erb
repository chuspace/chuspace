<div class='sm:w-2/3 mx-auto w-full'>
  <h3>Create a new post</h3>
  <p class='text-sm mt-2 text-secondary'>
    The post will be persisted into the connected publication
    repository draft directory.
  </p>

  <div class="text-sm breadcrumbs">
    <ul>
      <li>
        <%= link_to @post.publication.owner.username, user_path(@post.publication.owner), class: 'link link-primary' %>
      </li>
      <li>
        <%= link_to @post.publication.repository.fullname, user_publication_path(@post.publication.owner, @post.publication), class: 'link link-primary' %>
      </li>
    </ul>
  </div>

  <div class='mt-8'>
    <%= turbo_frame_tag @post do %>
      <%= render 'form', post: @post %>
    <% end %>
  </div>
</div>
